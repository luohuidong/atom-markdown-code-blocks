'use babel';

import MarkdownCodeBlocksView from '../lib/markdown-code-blocks-view';

describe('MarkdownCodeBlocksView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
